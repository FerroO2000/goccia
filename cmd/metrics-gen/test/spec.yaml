package: output
groups:
  - name: processor_stage
    metrics:
      - name: counted_items
        type: counter

      - name: current_items
        type: upDownCounter

  - name: egress_stage
    metrics:
      - name: counted_items
        type: counter

      - name: current_items
        type: upDownCounter

      - name: my_histogram
        type: histogram
