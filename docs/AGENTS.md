# Documentation Instructions

These instructions apply to files under `docs/`.

## Purpose

- Document Goccia clearly for library users.
- Keep explanations aligned with the current implementation. Treat the golang code as the source of truth.

## Writing Style

- Write all markdown headings and mermaid flowcharts in title case.

## Structure

- Order concept pages as `Pipeline -> Connectors -> Stages`.
- Add a forward link when a concept page has a natural next page.
- Keep navigation and prose links aligned with the intended reading order.

## Examples

- TODO: Define conventions for code snippets, diagrams, and sample pipelines.

## Validation

- Run `zensical build` from `docs/` after documentation changes.
