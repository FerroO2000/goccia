{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Goccia","text":"<p>Build composable streaming pipelines in Go.</p> <p>Goccia connects ingress, processor, and egress stages with typed ring buffers. Each stage can run with one worker or a dynamically scaled worker pool, while the pipeline coordinates startup, backpressure, graceful shutdown, and telemetry.</p> <pre><code>flowchart LR\n  ingress[Ingress] --&gt; processor[Processor]\n  processor --&gt; egress[Egress]</code></pre>","path":["Goccia"],"tags":[]},{"location":"#why-goccia","level":2,"title":"Why Goccia?","text":"<ul> <li>Compose typed pipelines from small reusable stages.</li> <li>Choose single-worker or pooled execution per stage.</li> <li>Preserve backpressure with bounded connectors.</li> <li>Drain queued work during graceful shutdown.</li> <li>Export metrics and traces through OpenTelemetry.</li> </ul>","path":["Goccia"],"tags":[]},{"location":"#start-here","level":2,"title":"Start Here","text":"<p>Read Get started to install the module and understand the shape of a pipeline. Continue with Pipeline concepts for the lifecycle model, or browse the examples for complete applications.</p> <p>The Go API is also available on <code>pkg.go.dev</code>.</p>","path":["Goccia"],"tags":[]},{"location":"examples/","level":1,"title":"Examples","text":"<p>Runnable applications live in the <code>examples</code> directory.</p> Example What it demonstrates CAN UDP ingress, Cannelloni decoding, reordering, CAN processing, and QuestDB egress CSV File ingress, CSV decoding and encoding, and file egress eBPF XDP ingress, custom processing, and sink egress File File ingress, custom processing, and file egress Kafka Ticker and Kafka ingress with Kafka egress","path":["Examples"],"tags":[]},{"location":"examples/#run-an-example","level":2,"title":"Run an Example","text":"<p>The examples share a Makefile:</p> <pre><code>cd examples\nmake &lt;example&gt; &lt;command&gt;\n</code></pre> <p>For example, run the CSV pipeline with:</p> <pre><code>cd examples\nmake csv up\nmake csv run\n</code></pre> <p>See the <code>examples</code> guide for the complete command list and infrastructure requirements.</p>","path":["Examples"],"tags":[]},{"location":"getting-started/","level":1,"title":"Get Started","text":"","path":["Get Started"],"tags":[]},{"location":"getting-started/#install","level":2,"title":"Install","text":"<p>Add Goccia to your Go module:</p> <pre><code>go get github.com/FerroO2000/goccia\n</code></pre>","path":["Get Started"],"tags":[]},{"location":"getting-started/#build-a-pipeline","level":2,"title":"Build a Pipeline","text":"<p>A pipeline is a directed graph of stages joined by typed connectors:</p> <pre><code>const connectorSize = 512\n\ntickerToCustom := connector.NewRingBuffer[*ingress.TickerMessage](connectorSize)\ncustomToSink := connector.NewRingBuffer[*ingress.TickerMessage](connectorSize)\n\ntickerCfg := ingress.NewTickerConfig()\ntickerStage := ingress.NewTickerStage(tickerToCustom, tickerCfg)\n\ncustomCfg := processor.NewGenericConfig(goccia.StageRunningModeSingle)\ncustomStage := processor.NewGenericStage(\n    handler,\n    tickerToCustom,\n    customToSink,\n    customCfg,\n)\n\nsinkStage := egress.NewSinkStage(customToSink)\n\npipeline := goccia.NewPipeline()\npipeline.AddStage(tickerStage)\npipeline.AddStage(customStage)\npipeline.AddStage(sinkStage)\n</code></pre> <p>Each output connector is the input connector of the following stage. During initialization, Goccia uses those connectors to build the stage graph.</p>","path":["Get Started"],"tags":[]},{"location":"getting-started/#run-and-close","level":2,"title":"Run and Close","text":"<p>Initialize the pipeline, run it in a goroutine, and close it after the run context is canceled:</p> <pre><code>if err := pipeline.Init(ctx); err != nil {\n    panic(err)\n}\n\ngo pipeline.Run(ctx)\n\n&lt;-ctx.Done()\npipeline.Close(closeCtx)\n</code></pre> <p><code>Pipeline.Close</code> waits for stages to drain in graph order before releasing their resources.</p>","path":["Get Started"],"tags":[]},{"location":"getting-started/#choose-a-running-mode","level":2,"title":"Choose a Running Mode","text":"<p>Processor and egress configurations accept a running mode:</p> <pre><code>cfg := processor.NewGenericConfig(goccia.StageRunningModeSingle)\n</code></pre> <p>Use <code>StageRunningModeSingle</code> when ordering or simplicity matters most. Use <code>StageRunningModePool</code> for parallel processing with bounded internal fan-out and fan-in queues.</p>","path":["Get Started"],"tags":[]},{"location":"concepts/connectors/","level":1,"title":"Connectors","text":"<p>Connectors join stages together. They transport messages, define the edges of the pipeline graph, and apply backpressure when consumers cannot keep up with producers. This page builds on the pipeline graph introduced in the previous concept guide.</p> <pre><code>flowchart LR\n  ingress[Ingress stage] --&gt;|connector| processor[Processor stage]\n  processor --&gt;|connector| egress[Egress stage]</code></pre>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#create-a-connector","level":2,"title":"Create a Connector","text":"<p>Goccia includes a bounded ring-buffer connector:</p> <pre><code>const connectorSize = 512\n\ntickerToCustom := connector.NewRingBuffer[*ingress.TickerMessage](connectorSize)\ncustomToSink := connector.NewRingBuffer[*ingress.TickerMessage](connectorSize)\n</code></pre> <p>Pass the same connector instance to the producing stage and the consuming stage:</p> <pre><code>tickerStage := ingress.NewTickerStage(tickerToCustom, tickerCfg)\ncustomStage := processor.NewGenericStage(\n    handler,\n    tickerToCustom,\n    customToSink,\n    customCfg,\n)\nsinkStage := egress.NewSinkStage(customToSink)\n</code></pre> <p>The generic type is the message body carried across the edge. Internally, Goccia wraps each body in a message envelope that stores lifecycle and telemetry metadata.</p>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#bounded-queues-and-backpressure","level":2,"title":"Bounded Queues and Backpressure","text":"<p>The built-in connector has a fixed capacity. A producer can continue writing while space is available. When the queue fills up, the next <code>Write</code> waits until a consumer reads a message:</p> <pre><code>producer faster than consumer\n-&gt; connector reaches capacity\n-&gt; producer waits\n-&gt; pressure propagates upstream\n</code></pre> <p>This behavior bounds memory usage and avoids silently dropping messages.</p> <p>Tip</p> <p>Start with a moderate capacity, such as <code>512</code>, and tune it using your workload metrics. A larger queue can absorb bursts, but it also retains more messages during shutdown and can increase end-to-end latency.</p>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#built-in-lock-free-ring-buffer","level":2,"title":"Built-In Lock-Free Ring Buffer","text":"<p>The built-in connector is optimized for stage-to-stage message passing. Its storage is a fixed-size circular array: after reaching the end of the array, the next write wraps back to the beginning and reuses slots that consumers have already released.</p> <pre><code>flowchart LR\n  producer[Producer] --&gt; head[Head index]\n  head --&gt; slots[Fixed-size ring]\n  slots --&gt; tail[Tail index]\n  tail --&gt; consumer[Consumer]</code></pre> <p>The public <code>connector.NewRingBuffer</code> constructor creates an SPSC (single-producer, single-consumer) buffer. This matches a normal pipeline edge: one stage writes messages and the following stage reads them.</p> <p>Goccia also uses specialized variants internally for pooled stages:</p> Variant Producers Consumers Used for SPSC One One Public connectors between stages SPMC One Many Internal fan-out from an input bridge to pooled workers MPSC Many One Internal fan-in from pooled workers to an output bridge","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#fast-path","level":3,"title":"Fast Path","text":"<p>When the ring has space or queued data, reads and writes use atomic operations instead of taking a mutex:</p> <ul> <li>The producer advances a <code>head</code> index.</li> <li>The consumer advances a <code>tail</code> index.</li> <li>An index mask maps those counters onto slots in the circular array.</li> <li>Cache-line padding separates frequently updated fields to reduce contention.</li> </ul> <p>The requested capacity is rounded up to a power of two. This allows Goccia to map an index to a slot using a bit mask instead of a remainder operation. For example, requesting capacity <code>500</code> creates a ring with capacity <code>512</code>.</p>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#waiting-path","level":3,"title":"Waiting Path","text":"<p>Lock-free does not mean busy-waiting forever. When a queue remains empty or full after a short spin period, the connector waits on a condition variable:</p> <pre><code>optimistic atomic operation\n-&gt; briefly yield while another goroutine may make progress\n-&gt; wait efficiently if the queue is still empty or full\n-&gt; wake when data, capacity, cancellation, or closure changes the state\n</code></pre> <p>This hybrid approach keeps the common path lightweight while avoiding needless CPU usage during longer idle or backpressure periods.</p>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#read-write-and-close","level":2,"title":"Read, Write, and Close","text":"<p>Every connector implements:</p> <pre><code>type Connector[T any] interface {\n    Write(item T) error\n    Read(ctx context.Context) (T, error)\n    Close()\n}\n</code></pre>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#write","level":3,"title":"Write","text":"<p><code>Write(item)</code> adds one message to the queue.</p> <ul> <li>It waits when the queue is full.</li> <li>It returns <code>connector.ErrClosed</code> if the connector has been closed.</li> <li>It does not accept a context, so cancellation alone does not interrupt a   blocked write.</li> </ul>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#read","level":3,"title":"Read","text":"<p><code>Read(ctx)</code> removes one message from the queue.</p> <ul> <li>It waits when the queue is empty.</li> <li>It returns <code>ctx.Err()</code> if the context is canceled while waiting.</li> <li>It returns <code>connector.ErrClosed</code> when the connector is closed and empty.</li> <li>It continues returning buffered messages after <code>Close()</code> until the queue has   been drained.</li> </ul>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#close","level":3,"title":"Close","text":"<p><code>Close()</code> marks the connector as closed and wakes blocked readers and writers. It is safe to call more than once.</p> <p>During normal pipeline usage, stages close their output connectors as part of the coordinated shutdown sequence. Application code generally calls <code>Pipeline.Close()</code> rather than closing connectors individually.</p>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#shutdown-example","level":2,"title":"Shutdown Example","text":"<p>If a connector contains two messages when its producer closes it, the consumer can still drain both messages:</p> <pre><code>producer closes connector\n-&gt; consumer reads queued message 1\n-&gt; consumer reads queued message 2\n-&gt; consumer Read returns connector.ErrClosed\n-&gt; consumer stage exits\n</code></pre> <p>This drain-before-exit behavior is essential to Goccia's graceful shutdown model.</p>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/connectors/#custom-connectors","level":2,"title":"Custom Connectors","text":"<p>The public <code>connector.Connector[T]</code> interface can be implemented by custom connectors. A custom implementation should preserve the same lifecycle contract:</p> <ul> <li><code>Read(ctx)</code> must return when its context is canceled while waiting.</li> <li><code>Close()</code> must wake blocked readers and writers.</li> <li>Buffered messages should remain readable after <code>Close()</code>.</li> <li>Writes after <code>Close()</code> must return <code>connector.ErrClosed</code>.</li> <li>Each connector instance must have stable pointer identity so Goccia can use   it to build the stage graph.</li> </ul> <p>Use the built-in ring buffer unless you need transport-specific behavior or a different queueing strategy.</p> <p>Next, read about the stages that produce and consume messages through these connectors.</p>","path":["Concepts","Connectors"],"tags":[]},{"location":"concepts/pipeline/","level":1,"title":"Pipeline Concepts","text":"<p>The pipeline coordinates a graph of stages. Its most important responsibility is graceful shutdown: upstream stages stop producing first, while downstream stages remain alive long enough to consume every queued and in-flight message.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#stage-graph","level":2,"title":"Stage Graph","text":"<p>Goccia models a pipeline as a directed acyclic graph:</p> <pre><code>flowchart LR\n  A[Ingress] --&gt; B[Processor]\n  B --&gt; C[Processor]\n  C --&gt; D[Egress]</code></pre> <p>A connector identifies each edge. Each node is a stage. The graph determines initialization order and shutdown propagation.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#building-the-graph","level":3,"title":"Building the Graph","text":"<p>When <code>Pipeline.Init()</code> runs, the pipeline builds the graph from connector identity:</p> <pre><code>stage output connector ID\n-&gt; matching downstream input connector ID\n-&gt; parent-child edge\n</code></pre> <p>A stage with no parent is an ingress node. The graph can have more than one ingress node, fan-out branches, and fan-in joins:</p> <pre><code>flowchart LR\n  A[Ingress A] --&gt; C[Processor C]\n  B[Ingress B] --&gt; D[Processor D]\n  C --&gt; E[Processor E]\n  D --&gt; E\n  E --&gt; F[Egress F]</code></pre> <p>The pipeline traverses this graph in topological order: every parent appears before its children. Initialization and final cleanup use this order.</p> <p>Note</p> <p>Add stages in upstream-to-downstream order. Graph construction resolves each input by looking up an output connector that has already been registered.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#pipeline-lifecycle","level":2,"title":"Pipeline Lifecycle","text":"<p>A pipeline moves through four phases:</p> <pre><code>flowchart LR\n  idle[Idle] --&gt;|AddStage| idle\n  idle --&gt;|Init| initialized[Initialized]\n  initialized --&gt;|Run| running[Running]\n  running --&gt;|Close| closed[Closed]</code></pre> <p>The typical application flow is:</p> <pre><code>pipeline := goccia.NewPipeline()\npipeline.AddStage(source)\npipeline.AddStage(processor)\npipeline.AddStage(destination)\n\nif err := pipeline.Init(ctx); err != nil {\n    return err\n}\n\ngo pipeline.Run(ctx)\n\n&lt;-ctx.Done()\npipeline.Close(closeCtx)\n</code></pre> <p><code>Run()</code> blocks until every stage has returned from its own <code>Run()</code> method. Applications commonly start it in a goroutine and call <code>Close()</code> after receiving a shutdown signal.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#initialization","level":3,"title":"Initialization","text":"<p><code>Pipeline.Init(ctx)</code>:</p> <ol> <li>Builds the stage graph.</li> <li>Traverses stages from upstream to downstream.</li> <li>Calls <code>Init(ctx)</code> on each stage.</li> </ol> <p>Each stage initializes its environment before its runner. This is where configuration validation, metrics setup, and stage-specific resources are created.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#running","level":3,"title":"Running","text":"<p><code>Pipeline.Run(ctx)</code> starts one run goroutine for every stage. Processor and egress stages also receive a small watcher goroutine that controls when their run context is canceled.</p> <p>All stage run goroutines start together. Shutdown order is enforced later by context cancellation and <code>runDoneCh</code>, not by delaying the startup of downstream stages.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#backpressure","level":2,"title":"Backpressure","text":"<p>Connectors are bounded queues. When a queue is full, its producer waits until a consumer reads a message. This bounds memory usage and propagates pressure upstream instead of silently dropping work.</p> <p>Backpressure is why shutdown cannot cancel every stage at once. If an upstream stage is blocked in <code>Write()</code>, its downstream consumer must remain alive until the write completes or the connector closes.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#context-cancellation-chain","level":2,"title":"Context Cancellation Chain","text":"<p>Shutdown cancellation propagates through the graph in the same direction as messages:</p> <pre><code>ingress stops\n-&gt; its Run returns\n-&gt; direct children are canceled\n-&gt; children drain and return\n-&gt; their children are canceled\n-&gt; repeat until every leaf has stopped\n</code></pre> <p>This is not Go's ordinary parent-context propagation. The pipeline constructs the cancellation chain deliberately.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#context-roles","level":3,"title":"Context Roles","text":"<p><code>Pipeline.Run(ctx)</code> creates two context families:</p> Context Used by Cancellation behavior Root run context Ingress stages Derived from the caller's <code>ctx</code>; canceled by the caller or <code>Pipeline.Close()</code> Detached stage context Processor and egress stages Detached from the caller's cancellation, then wrapped with a dedicated cancel function <p>In simplified form:</p> <pre><code>runRootCtx, cancelRunRootCtx := context.WithCancel(ctx)\ndetachedCtx := context.WithoutCancel(ctx)\n\nfor each stage {\n    runCtx := runRootCtx\n\n    if stage is not ingress {\n        runCtx, stage.cancelRunCtx = context.WithCancel(detachedCtx)\n    }\n\n    go runStage(runCtx, stage)\n}\n</code></pre> <p>The detached downstream contexts are essential. When the external signal cancels <code>ctx</code>, ingress stages should stop reading from external sources. Processors and egress stages must stay alive because ingress stages may still be finishing blocked writes or publishing final messages.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#per-stage-completion-signal","level":3,"title":"Per-Stage Completion Signal","text":"<p>Every graph node owns a <code>runDoneCh</code>. A stage closes this channel only after its <code>Run()</code> method has returned:</p> <pre><code>stage.Run(ctx)\n-&gt; stage drains its local work\n-&gt; stage.Run returns\n-&gt; close stage.runDoneCh\n</code></pre> <p>For a non-ingress stage, the pipeline starts a watcher:</p> <pre><code>for parent := range stage.parents() {\n    &lt;-parent.runDoneCh\n}\n\nstage.cancelRun()\n</code></pre> <p>The watcher waits for every immediate parent. Only then does it cancel the stage's dedicated run context.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#linear-example","level":3,"title":"Linear Example","text":"<p>Consider a simple pipeline:</p> <pre><code>flowchart LR\n  A[Ingress A] --&gt; B[Processor B]\n  B --&gt; C[Egress C]</code></pre> <p>When the application receives a shutdown signal:</p> <pre><code>external context is canceled\n-&gt; A run context is canceled\n-&gt; A stops reading its external source\n-&gt; A finishes any current output write\n-&gt; A.Run returns and closes A.runDoneCh\n-&gt; B watcher cancels B run context\n-&gt; B drains queued and in-flight messages\n-&gt; B.Run returns and closes B.runDoneCh\n-&gt; C watcher cancels C run context\n-&gt; C drains final deliveries\n-&gt; C.Run returns and closes C.runDoneCh\n</code></pre> <p>At each step, the downstream consumer stays active until its upstream producer has finished.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#fan-in-example","level":3,"title":"Fan-In Example","text":"<p>A fan-in stage must not stop when only one parent finishes:</p> <pre><code>flowchart LR\n  A[Ingress A] --&gt; C[Processor C]\n  B[Ingress B] --&gt; C\n  C --&gt; D[Egress D]</code></pre> <p>The cancellation chain is:</p> <pre><code>A.Run returns ----\\\n                   -&gt; wait for both parents -&gt; cancel C\nB.Run returns ----/\n\nC.Run drains and returns\n-&gt; cancel D\n</code></pre> <p>If <code>A</code> finishes before <code>B</code>, <code>C</code> remains active so it can continue consuming messages from <code>B</code>. The same rule applies at every join in a larger graph.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#fan-out-example","level":3,"title":"Fan-Out Example","text":"<p>When one parent has multiple children, each child has its own watcher and dedicated run context:</p> <pre><code>flowchart LR\n  A[Processor A] --&gt; B[Egress B]\n  A --&gt; C[Egress C]</code></pre> <p>After <code>A.Run()</code> returns, both <code>B</code> and <code>C</code> can begin draining independently. One branch does not wait for the other branch unless they meet again at a downstream fan-in node.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#graceful-shutdown","level":2,"title":"Graceful Shutdown","text":"<p>The pipeline-level cancellation wave composes with each stage's local drain rules:</p> <pre><code>stop upstream producer\n-&gt; let producer publish final output\n-&gt; cancel direct downstream consumers\n-&gt; let consumers drain queued and in-flight work\n-&gt; continue downstream\n</code></pre> <p>For a pooled processor stage, the local drain adds internal fan-out and fan-in queues:</p> <pre><code>flowchart LR\n  input[External input] --&gt; inbridge[Input bridge]\n  inbridge --&gt; fanout[Fan-out queue]\n  fanout --&gt; workers[Worker executors]\n  workers --&gt; fanin[Fan-in queue]\n  fanin --&gt; outbridge[Output bridge]\n  outbridge --&gt; output[External output]</code></pre> <p>After the stage run context is canceled:</p> <pre><code>input bridge stops and closes fan-out\n-&gt; worker executors drain fan-out and finish in-flight work\n-&gt; runner waits for every executor\n-&gt; runner closes fan-in\n-&gt; output bridge drains fan-in\n-&gt; output bridge closes the external output connector\n-&gt; stage Run returns\n</code></pre> <p>This ordering keeps downstream consumers alive while producers can still be blocked by backpressure.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/pipeline/#closing-resources","level":2,"title":"Closing Resources","text":"<p><code>Pipeline.Close(ctx)</code> starts the shutdown if it has not already begun:</p> <pre><code>cancel root run context\n-&gt; wait for each stage Run to return\n-&gt; call each stage Close(ctx) in topological order\n</code></pre> <p>The root cancellation affects ingress stages directly. The downstream cancellation chain then proceeds through <code>runDoneCh</code> as described above.</p> <p><code>Run()</code> and <code>Close()</code> have different responsibilities:</p> Method Responsibility <code>Run()</code> Process messages and complete the graceful drain <code>Close(ctx)</code> Release resources after draining has completed <p>For example, an ingress runner closes its output connector during cleanup, and an egress environment may flush or close an external client.</p> <p>The context passed to <code>Pipeline.Close(ctx)</code> is the application's cleanup context. It is separate from the canceled run context so resource cleanup can still perform final operations. Applications can give it a deadline:</p> <pre><code>closeCtx, cancelClose := context.WithTimeout(context.Background(), 10*time.Second)\ndefer cancelClose()\n\npipeline.Close(closeCtx)\n</code></pre> <p>Important</p> <p>Canceling the run context stops and drains stage execution. Call <code>Pipeline.Close()</code> as well so the pipeline waits for that drain and releases stage resources.</p> <p>Next, read about the connectors that define graph edges and carry messages between stages.</p>","path":["Concepts","Pipeline Concepts"],"tags":[]},{"location":"concepts/stages/","level":1,"title":"Stages","text":"<p>Stages are the active nodes of a Goccia pipeline. A stage reads from zero or more input connectors, performs work, and writes to zero or more output connectors. This page builds on the pipeline lifecycle and connector behavior introduced in the previous concept guides.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#the-base-stage","level":2,"title":"The Base Stage","text":"<p>Every stage kind embeds a <code>BaseStage</code>. This common shell exposes the stage to the pipeline and delegates execution to two main components: a runner and an environment.</p> <pre><code>type BaseStage[Env env.Env] struct {\n    kind Kind\n    name string\n\n    env Env\n    runner Runner[Env]\n}\n</code></pre> <pre><code>flowchart LR\n  stage[BaseStage] --&gt; runner[Runner]\n  stage --&gt; env[Environment]\n  env --&gt; config[Configuration]\n  env --&gt; telemetry[Telemetry and Metrics]</code></pre> <p><code>BaseStage</code> also stores the stage kind and name used by the pipeline and telemetry. Its lifecycle methods call the environment and runner in ownership order:</p> <pre><code>Init:\n  initialize environment\n  attach environment to runner\n  initialize runner\n\nRun:\n  run runner\n\nClose:\n  close runner\n  close environment\n</code></pre>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#environment","level":3,"title":"Environment","text":"<p>The environment is shared stage state. It is available to the runner and, for worker-backed stages, to every worker executor. A typical environment contains:</p> <ul> <li>The stage configuration.</li> <li>Telemetry and metrics.</li> <li>Resources shared across the stage, such as a network connections.</li> </ul> <p><code>BaseStage</code> creates telemetry from the stage kind and name, then attaches it to the environment before initialization. The environment uses that telemetry for logs, traces, and metrics.</p> <p>The base environment validates its configuration and initializes metrics in <code>Init()</code>. Individual stage environments can extend that lifecycle to create resources from their configuration, and release those resources in <code>Close()</code>.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#runner","level":3,"title":"Runner","text":"<p>A stage runner implements:</p> <pre><code>type Runner[Env env.Env] interface {\n    SetEnvironment(env Env)\n    Init(ctx context.Context) error\n    Run(ctx context.Context)\n    Close(ctx context.Context)\n    Inputs() []uintptr\n    Outputs() []uintptr\n}\n</code></pre> <p>The runner owns active execution. Its <code>Inputs()</code> and <code>Outputs()</code> methods expose connector IDs so the pipeline can discover graph edges. Its remaining methods define how work is initialized, run, drained, and closed.</p> <p>A stage can provide a custom runner when it needs source-specific behavior or special lifecycle rules. Processor and egress stages can also use the generic single or pooled runners described later on this page.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#stage-kinds","level":2,"title":"Stage Kinds","text":"<p>The common <code>BaseStage</code> shell is embedded by three stage variants:</p> Kind Inputs Outputs Purpose Ingress None One or more Produces messages from an external source Processor One or more One or more Transforms, filters, or routes messages Egress One or more None Delivers messages to an external destination","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#ingress-stages","level":2,"title":"Ingress Stages","text":"<p>Ingress stages are the simplest variant. They do not receive messages from another stage, and they do not use the generic worker-pool machinery. Instead, an ingress stage implements a custom runner directly:</p> <pre><code>flowchart LR\n  source[External source] --&gt; runner[Ingress runner]\n  runner --&gt; output[Output connector]</code></pre> <p>For example, the ticker ingress runner waits for a timer, creates a message, and writes it to its output connector:</p> <pre><code>func (tr *tickerRunner) Run(ctx context.Context) {\n    defer close(tr.runDone)\n\n    ticker := time.NewTicker(tr.Config.Interval)\n    defer ticker.Stop()\n\n    ticks := 0\n    for {\n        ticks++\n\n        select {\n        case &lt;-ctx.Done():\n            return\n        case &lt;-ticker.C:\n            msgOut := tr.handleTrigger(ctx, ticks)\n            if err := tr.outConnector.Write(msgOut); err != nil {\n                msgOut.Destroy()\n            }\n        }\n    }\n}\n</code></pre> <p>An ingress runner owns the details of its source.</p> <p>The generic ingress stage wraps the runner and environment in <code>BaseStage</code>:</p> <pre><code>stage.NewIngressStageFromRunner(\n    \"ticker\",\n    newTickerEnv(cfg),\n    newTickerRunner(output),\n)\n</code></pre> <p>Specialized processor and egress stages can use the same custom-runner path. For example, a reorder buffer can own state and flushing rules that do not fit a one-message-at-a-time worker.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#worker-backed-stages","level":2,"title":"Worker-Backed Stages","text":"<p>Most processor and egress stages can be expressed as one operation applied to each incoming message. Goccia builds these stages from workers:</p> <pre><code>flowchart TB\n  stage[BaseStage] --&gt; runner[Runner]\n  stage --&gt; env[Environment]\n  env --&gt; config[Configuration]\n  env --&gt; telemetry[Telemetry and metrics]\n\n  runner --&gt; backend[Runner backend]\n  backend --&gt; executor[Worker executor]\n  executor --&gt; handler[Worker handler]\n  handler --&gt; worker[Worker]\n\n  backend -. uses .-&gt; env\n  executor -. uses .-&gt; env\n  worker -. receives .-&gt; env\n  handler -. records .-&gt; telemetry\n\n  classDef base fill:#dbeafe,stroke:#2563eb,color:#172554\n  classDef worker fill:#ffedd5,stroke:#ea580c,color:#431407\n\n  class stage,runner,env,config,telemetry base\n  class backend,executor,handler,worker worker</code></pre> <p>Each layer has a distinct responsibility:</p> Component Responsibility Stage Exposes the stage lifecycle and graph identity Runner Chooses single or pooled execution and coordinates shutdown Runner backend Adapts processor or egress behavior to the generic runner Worker executor Repeatedly executes one worker instance Worker handler Performs the per-message bookkeeping around the worker Worker Contains the stage-specific processing or delivery logic Environment Stores shared configuration, telemetry, metrics, and resources","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#runner-backend","level":3,"title":"Runner Backend","text":"<p>The runner backend is the adapter between generic stage orchestration and the kind-specific worker path:</p> <pre><code>type stageRunnerBackend[Env env.Env, W worker.Worker[Env]] interface {\n    setEnvironment(env Env)\n    newWorkerExecutor(workerID int) *worker.Executor[Env, W]\n    runInputBridge(ctx context.Context)\n    runOutputBridge(ctx context.Context)\n    closeOutput()\n    inputConnectorID() uintptr\n    outputConnectorID() uintptr\n}\n</code></pre> <p>A processor backend has an input and an output. It can create both an input reader and an output writer for each worker executor.</p> <p>An egress backend has only an input. Its output bridge and output close operations are intentionally empty because delivery terminates outside the pipeline.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#worker-executor","level":3,"title":"Worker Executor","text":"<p>A worker executor owns one worker instance and repeatedly invokes its handler:</p> <pre><code>executor\n-&gt; read one message\n-&gt; invoke worker-specific logic\n-&gt; publish or deliver the result\n-&gt; repeat\n</code></pre> <p>The executor has two execution methods:</p> Method Used by Stops when <code>Run(ctx)</code> Single runner Input read observes cancellation or connector closure <code>RunPooled(ctx, stopCh, pendingCounter)</code> Pooled runner Internal input closes or the scaler requests scale-down <p>Each pooled executor receives its own worker instance. The environment is shared by all executors in the stage.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#processor-handler","level":3,"title":"Processor Handler","text":"<p>A processor worker implements:</p> <pre><code>type Processor[Env env.Env, In, Out msgBody] interface {\n    Worker[Env]\n    Handle(ctx context.Context, msgIn *msg[In]) (*msg[Out], error)\n}\n</code></pre> <p>The processor handler wraps each call to <code>Handle</code> with common behavior:</p> <ol> <li>Read an input message.</li> <li>Restore its tracing context.</li> <li>Invoke the worker.</li> <li>Record processing errors and metrics.</li> <li>Propagate timestamp metadata to the output message.</li> <li>Destroy dropped messages or write produced messages downstream.</li> <li>Destroy the consumed input message.</li> </ol>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#egress-handler","level":3,"title":"Egress Handler","text":"<p>An egress worker implements:</p> <pre><code>type Egress[Env env.Env, In msgBody] interface {\n    Worker[Env]\n    Deliver(ctx context.Context, msgIn *msg[In]) error\n}\n</code></pre> <p>The egress handler reads a message, restores its tracing context, invokes <code>Deliver</code>, records metrics, and destroys the consumed message. There is no output connector.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#single-execution-mode","level":2,"title":"Single Execution Mode","text":"<p>Single mode creates one worker executor and attaches it directly to the external connectors:</p> <pre><code>flowchart LR\n  input[External input] --&gt; executor[Worker executor]\n  executor --&gt; output[External output]</code></pre> <p>The stage does not allocate internal queues or bridge goroutines. The external connector provides buffering and backpressure.</p> <p>Use single mode when:</p> <ul> <li>Message ordering must be preserved.</li> <li>Processing is lightweight.</li> <li>The external dependency should receive requests serially.</li> <li>A stage-specific resource is not safe to use concurrently.</li> </ul>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#pooled-execution-mode","level":2,"title":"Pooled Execution Mode","text":"<p>Pool mode adds internal queues and bridges around multiple worker executors:</p> <pre><code>flowchart LR\n  input[External input] --&gt; inbridge[Input bridge]\n  inbridge --&gt; fanout[Fan-out queue SPMC]\n  fanout --&gt; executors[Worker executors]\n  executors --&gt; fanin[Fan-in queue MPSC]\n  fanin --&gt; outbridge[Output bridge]\n  outbridge --&gt; output[External output]</code></pre> <p>The internal queue variants match their traffic:</p> Queue Producers Consumers Purpose Fan-out One input bridge Many worker executors Distribute incoming work Fan-in Many worker executors One output bridge Gather processed messages <p>An egress pool does not need a fan-in queue or output bridge:</p> <pre><code>flowchart LR\n  input[External input] --&gt; inbridge[Input bridge]\n  inbridge --&gt; fanout[Fan-out queue SPMC]\n  fanout --&gt; executors[Worker executors]\n  executors --&gt; destination[External destination]</code></pre>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#input-bridge","level":3,"title":"Input Bridge","text":"<p>The input bridge reads from the external connector and writes to the internal fan-out queue. When it exits, it closes the fan-out queue. That closure is the terminal signal that lets pooled executors drain remaining messages and stop.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#output-bridge","level":3,"title":"Output Bridge","text":"<p>The output bridge reads from the internal fan-in queue and writes to the external output connector. It must remain alive until every executor has finished. Otherwise, executors could block forever while writing to a full fan-in queue with no remaining consumer.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#scaler","level":3,"title":"Scaler","text":"<p>Each pooled runner owns a scaler. It:</p> <ul> <li>Starts the configured initial number of executors.</li> <li>Tracks active executors and pending tasks.</li> <li>Requests scale-up when queue depth per worker exceeds the configured target.</li> <li>Requests scale-down gradually when load falls below the active worker count.</li> <li>Stops executor creation when the stage run context is canceled.</li> </ul> <p>Pool configuration controls worker counts, queue sizes, and scaling behavior:</p> <pre><code>cfg := processor.NewGenericConfig(goccia.StageRunningModePool)\ncfg.Stage.Pool.MinWorkers = 2\ncfg.Stage.Pool.MaxWorkers = 8\ncfg.Stage.Pool.InputQueueSize = 512\ncfg.Stage.Pool.OutputQueueSize = 512\n</code></pre>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#pooled-shutdown","level":2,"title":"Pooled Shutdown","text":"<p>Pooled shutdown is deliberately ordered. Connector closure, rather than stage context cancellation alone, drives the drain:</p> <pre><code>stage run context is canceled\n-&gt; stop accepting scaler start requests\n-&gt; input bridge exits and closes fan-out\n-&gt; worker executors drain fan-out and finish in-flight work\n-&gt; runner waits for every executor\n-&gt; runner closes fan-in\n-&gt; output bridge drains fan-in and closes external output\n-&gt; runner closes the scaler\n-&gt; stage Run returns\n</code></pre> <p>Worker execution and output-bridge reads use a detached context during this phase. This is intentional: canceling them immediately could strand messages in an internal queue or leave an executor blocked by downstream backpressure.</p> <p>The pipeline lifecycle builds on this local stage behavior by shutting down stages in message-flow order.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"concepts/stages/#choosing-a-model","level":2,"title":"Choosing a Model","text":"<p>Use a custom runner when the stage has source-specific behavior, owns complex state, or needs lifecycle rules that do not fit independent per-message work.</p> <p>Use a worker-backed stage in single mode when one executor is enough or ordering matters.</p> <p>Use a worker-backed stage in pool mode when messages can be processed independently and the workload benefits from concurrency.</p>","path":["Concepts","Stages"],"tags":[]},{"location":"stages/","level":1,"title":"Built-in Stages","text":"<p>Goccia ships stages in three categories:</p> <ul> <li>Ingress: create messages from external sources.</li> <li>Processor: transform, filter, fan out, or reorder messages.</li> <li>Egress: deliver messages to external destinations.</li> </ul>","path":["Built-in Stages"],"tags":[]},{"location":"stages/#message-interfaces","level":2,"title":"Message Interfaces","text":"<p>Every message body implements Goccia's standard <code>message.Body</code> contract by providing <code>Destroy()</code>. Some built-in message types implement additional interfaces:</p> Interface Extra method Used by <code>message.Serializable</code> <code>GetBytes() []byte</code> Byte-oriented processors and TCP, UDP, or file egress stages <code>message.ReOrderable</code> <code>GetSequenceNumber() uint64</code> <code>ROBStage</code> <code>processor.CANMessageCarrier</code> <code>GetRawMessages() []CANRawMessage</code> <code>CANStage</code>","path":["Built-in Stages"],"tags":[]},{"location":"stages/#execution-badges","level":2,"title":"Execution Badges","text":"<p>Pool-capable</p> <p>This badge marks stages that can use <code>StageRunningModePool</code>. It links to the pooled execution mode concept section for the queueing, scaling, and shutdown model.</p> <p>Most processor and egress stages use a common running-mode configuration:</p> <pre><code>cfg := processor.NewGenericConfig(goccia.StageRunningModePool)\n</code></pre> <p><code>StageRunningModeSingle</code> runs one executor. <code>StageRunningModePool</code> enables the generic worker pool and exposes <code>cfg.Stage.Pool</code> for worker counts, queue sizes, and auto-scaling. Stages with custom runners, such as ingress stages, <code>tee</code>, <code>rob</code>, <code>sink</code>, file egress, and TCP egress, document their own execution model.</p> <p>For lifecycle details, see Stages.</p>","path":["Built-in Stages"],"tags":[]},{"location":"stages/egress/","level":1,"title":"Egress Stages","text":"<p>Egress stages consume messages and do not write to downstream connectors.</p> Stage Constructor Capability Destination Generic <code>egress.NewGenericStage</code> Pool-capable User-defined handler Sink <code>egress.NewSinkStage</code> Single runner Drop and destroy messages TCP <code>egress.NewTCPStage</code> Single worker TCP connection UDP <code>egress.NewUDPStage</code> Pool-capable UDP socket File <code>egress.NewFileStage</code> Single worker Append-only file Kafka <code>egress.NewKafkaStage</code> Pool-capable Kafka producer QuestDB <code>egress.NewQuestDBStage</code> Pool-capable QuestDB line protocol","path":["Built-in Stages","Egress","Egress Stages"],"tags":[]},{"location":"stages/egress/file/","level":1,"title":"File Egress","text":"<p><code>FileStage</code> appends serializable message bytes to a file.</p> <pre><code>cfg := egress.NewFileConfig(\"./data/out/out.txt\")\nstage := egress.NewFileStage(in, cfg)\n</code></pre>","path":["Built-in Stages","Egress","File Egress"],"tags":[]},{"location":"stages/egress/file/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Egress","File Egress"],"tags":[]},{"location":"stages/egress/file/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body requirement: <code>message.Serializable</code>.</p> <p>The stage appends the byte slice returned by <code>GetBytes()</code> to the configured file.</p> <p>This egress stage produces no downstream output message.</p>","path":["Built-in Stages","Egress","File Egress"],"tags":[]},{"location":"stages/egress/file/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Path</code> constructor arg Output file path. Parent directories are created automatically. <code>BufferSize</code> <code>4096</code> <code>bufio.Writer</code> buffer size. <code>FlushThresholdPercentage</code> <code>0.75</code> Flush when pending bytes reach this fraction of <code>BufferSize</code>. <code>FlushDeadline</code> <code>time.Second</code> Periodic flush interval. <p>File egress always runs in single mode and does not expose the generic worker-pool config.</p>","path":["Built-in Stages","Egress","File Egress"],"tags":[]},{"location":"stages/egress/file/#internals","level":2,"title":"Internals","text":"<p>The stage uses <code>os.File</code> and <code>bufio.Writer</code> from the standard library. It opens the file append-only, writes each message byte slice, flushes on threshold or deadline, and syncs/closes the file during stage close.</p>","path":["Built-in Stages","Egress","File Egress"],"tags":[]},{"location":"stages/egress/generic/","level":1,"title":"Generic Egress","text":"<p><code>GenericStage</code> runs a user-provided handler for each input message and terminates the pipeline branch.</p> <p>Pool-capable</p> <pre><code>cfg := egress.NewGenericConfig(\"publish_metrics\", goccia.StageRunningModePool)\nstage := egress.NewGenericStage(handler, in, cfg)\n</code></pre>","path":["Built-in Stages","Egress","Generic Egress"],"tags":[]},{"location":"stages/egress/generic/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Egress","Generic Egress"],"tags":[]},{"location":"stages/egress/generic/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body type: the handler's <code>In</code> type parameter.</p> <p><code>GenericStage</code> only requires <code>In</code> to implement <code>message.Body</code>. Any additional interfaces depend on the concrete input body type and on the logic implemented by the handler.</p> <p>This egress stage produces no downstream output message.</p>","path":["Built-in Stages","Egress","Generic Egress"],"tags":[]},{"location":"stages/egress/generic/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling. <code>Name</code> constructor arg, or <code>\"generic\"</code> when empty Stage name used in telemetry and the pipeline graph.","path":["Built-in Stages","Egress","Generic Egress"],"tags":[]},{"location":"stages/egress/generic/#handler","level":2,"title":"Handler","text":"<p>The handler implements <code>Init(ctx)</code>, <code>Handle(ctx, msgIn)</code>, <code>Close()</code>, and <code>SetTelemetry(tel)</code>. Embed <code>egress.GenericHandlerBase</code> when you only need to implement <code>Handle</code>.</p> <p><code>Handle</code> receives the message body and returns an error. Returning an error marks the delivery as failed for that message.</p>","path":["Built-in Stages","Egress","Generic Egress"],"tags":[]},{"location":"stages/egress/generic/#internals","level":2,"title":"Internals","text":"<p>The stage uses Goccia's generic worker-backed egress runner. Each worker restores the message context, starts a handler span, and calls the handler without writing to an output connector.</p>","path":["Built-in Stages","Egress","Generic Egress"],"tags":[]},{"location":"stages/egress/kafka/","level":1,"title":"Kafka Egress","text":"<p><code>KafkaStage</code> writes <code>KafkaMessage</code> values to Kafka.</p> <p>Pool-capable</p> <pre><code>cfg := egress.DefaultKafkaConfig(goccia.StageRunningModePool)\nstage := egress.NewKafkaStage(in, cfg)\n</code></pre>","path":["Built-in Stages","Egress","Kafka Egress"],"tags":[]},{"location":"stages/egress/kafka/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Egress","Kafka Egress"],"tags":[]},{"location":"stages/egress/kafka/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body type: <code>*egress.KafkaMessage</code>.</p> Field or method Description <code>Topic</code> Kafka topic to write. <code>Key</code> Kafka record key. <code>Value</code> Kafka record value. <code>AddHeader(key, value)</code> Adds a Kafka header before delivery. <p>Additional input interfaces: none. <code>KafkaMessage</code> only implements the standard <code>message.Body</code> contract.</p> <p>This egress stage produces no downstream output message.</p>","path":["Built-in Stages","Egress","Kafka Egress"],"tags":[]},{"location":"stages/egress/kafka/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling. <code>Brokers</code> <code>[]string{\"localhost:9092\"}</code> Kafka broker addresses. <code>Balancer</code> <code>kafka.RoundRobin</code> Partition balancer. <code>MaxAttempts</code> <code>10</code> Delivery attempt limit. <code>WriteBackoffMin</code> <code>100 * time.Millisecond</code> Minimum write retry backoff. <code>WriteBackoffMax</code> <code>1 * time.Second</code> Maximum write retry backoff. <code>BatchSize</code> <code>100</code> Message count that triggers a batch flush. <code>BatchBytes</code> <code>1048576</code> Byte size that triggers a batch flush. <code>BatchTimeout</code> <code>1 * time.Second</code> Time limit for incomplete batches. <code>ReadTimeout</code> <code>10 * time.Second</code> Writer read timeout. <code>WriteTimeout</code> <code>10 * time.Second</code> Writer write timeout. <code>RequiredAcks</code> <code>kafka.RequireNone</code> Produce acknowledgement requirement. <code>Async</code> <code>true</code> Non-blocking writes. Errors are ignored by kafka-go in async mode. <code>Compression</code> <code>kafka.Snappy</code> Kafka compression codec. <code>Transport</code> <code>nil</code> Optional kafka-go transport. <code>AllowAutoTopicCreation</code> <code>true</code> Allow missing topics to be created.","path":["Built-in Stages","Egress","Kafka Egress"],"tags":[]},{"location":"stages/egress/kafka/#internals","level":2,"title":"Internals","text":"<p>The stage uses <code>github.com/segmentio/kafka-go</code>. It creates a <code>kafka.Writer</code>, injects OpenTelemetry trace context into Kafka headers, then calls <code>WriteMessages(ctx, kafka.Message{...})</code>.</p>","path":["Built-in Stages","Egress","Kafka Egress"],"tags":[]},{"location":"stages/egress/questdb/","level":1,"title":"QuestDB Egress","text":"<p><code>QuestDBStage</code> writes rows to QuestDB.</p> <p>Pool-capable</p> <pre><code>cfg := egress.NewQuestDBConfig(goccia.StageRunningModePool)\ncfg.Address = \"localhost:9000\"\nstage := egress.NewQuestDBStage(in, cfg)\n</code></pre>","path":["Built-in Stages","Egress","QuestDB Egress"],"tags":[]},{"location":"stages/egress/questdb/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Egress","QuestDB Egress"],"tags":[]},{"location":"stages/egress/questdb/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body type: <code>*egress.QuestDBMessage</code>.</p> <p>Build a <code>QuestDBMessage</code> from rows:</p> <pre><code>msg := egress.NewQuestDBMessage()\nrow := egress.NewQuestDBRow(\"can_signals\")\nrow.AddSymbol(egress.NewQuestDBSymbol(\"name\", \"rpm\"))\nrow.AddColumn(egress.NewQuestDBFloatColumn(\"value\", 8123.4))\nmsg.AddRow(row)\n</code></pre> <p>Supported column types are bool, int, long, float, string, and timestamp. Symbols are stored separately because QuestDB symbols must be written before ordinary columns.</p> <p>Additional input interfaces: none. <code>QuestDBMessage</code> only implements the standard <code>message.Body</code> contract.</p> <p>This egress stage produces no downstream output message.</p>","path":["Built-in Stages","Egress","QuestDB Egress"],"tags":[]},{"location":"stages/egress/questdb/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling. <code>Address</code> <code>\"localhost:9000\"</code> QuestDB endpoint address.","path":["Built-in Stages","Egress","QuestDB Egress"],"tags":[]},{"location":"stages/egress/questdb/#internals","level":2,"title":"Internals","text":"<p>The stage uses <code>github.com/questdb/go-questdb-client/v3</code>. Initialization builds a line-sender pool with HTTP transport, auto-flush rows set to <code>75000</code>, and a one-second retry timeout. Each worker gets a sender from the pool and writes rows with the message envelope timestamp.</p>","path":["Built-in Stages","Egress","QuestDB Egress"],"tags":[]},{"location":"stages/egress/sink/","level":1,"title":"Sink Egress","text":"<p><code>SinkStage</code> consumes and destroys every incoming message.</p> <pre><code>stage := egress.NewSinkStage(in)\n</code></pre>","path":["Built-in Stages","Egress","Sink Egress"],"tags":[]},{"location":"stages/egress/sink/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Egress","Sink Egress"],"tags":[]},{"location":"stages/egress/sink/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body type: any <code>T</code> that implements <code>message.Body</code>.</p> <p>Additional input interfaces: none required. If the input body implements <code>message.Serializable</code>, <code>message.ReOrderable</code>, or another interface, <code>SinkStage</code> does not inspect it; it only destroys the incoming envelope.</p> <p>This egress stage produces no downstream output message.</p>","path":["Built-in Stages","Egress","Sink Egress"],"tags":[]},{"location":"stages/egress/sink/#configuration","level":2,"title":"Configuration","text":"<p><code>SinkStage</code> has no config object. It is intended mainly for tests, examples, and benchmarks where the pipeline needs a terminal consumer.</p>","path":["Built-in Stages","Egress","Sink Egress"],"tags":[]},{"location":"stages/egress/sink/#internals","level":2,"title":"Internals","text":"<p>The stage uses a custom single runner. It repeatedly reads from the input connector and calls <code>Destroy()</code> on each message envelope until the connector is closed and drained.</p>","path":["Built-in Stages","Egress","Sink Egress"],"tags":[]},{"location":"stages/egress/tcp/","level":1,"title":"TCP Egress","text":"<p><code>TCPStage</code> writes serializable message bytes to one TCP connection.</p> <pre><code>cfg := egress.NewTCPConfig()\ncfg.IPAddr = \"127.0.0.1\"\ncfg.Port = 20000\nstage := egress.NewTCPStage(in, cfg)\n</code></pre>","path":["Built-in Stages","Egress","TCP Egress"],"tags":[]},{"location":"stages/egress/tcp/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Egress","TCP Egress"],"tags":[]},{"location":"stages/egress/tcp/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body requirement: <code>message.Serializable</code>.</p> <p>The stage writes the byte slice returned by <code>GetBytes()</code> to the TCP connection.</p> <p>This egress stage produces no downstream output message.</p>","path":["Built-in Stages","Egress","TCP Egress"],"tags":[]},{"location":"stages/egress/tcp/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>IPAddr</code> <code>\"127.0.0.1\"</code> Destination address. <code>Port</code> <code>20000</code> Destination TCP port. <code>WriteTimeout</code> <code>10 * time.Second</code> Deadline applied before each write. <p>TCP egress always runs in single mode and does not expose the generic worker-pool config.</p>","path":["Built-in Stages","Egress","TCP Egress"],"tags":[]},{"location":"stages/egress/tcp/#internals","level":2,"title":"Internals","text":"<p>The stage uses Go's standard <code>net</code> package. Initialization dials a <code>net.TCPConn</code>; the worker sets a write deadline, writes the full byte slice returned by <code>GetBytes()</code>, and closes the connection when the stage closes.</p>","path":["Built-in Stages","Egress","TCP Egress"],"tags":[]},{"location":"stages/egress/udp/","level":1,"title":"UDP Egress","text":"<p><code>UDPStage</code> sends each serializable message as one UDP datagram.</p> <p>Pool-capable</p> <pre><code>cfg := egress.NewUDPConfig(goccia.StageRunningModePool)\ncfg.IPAddr = \"127.0.0.1\"\ncfg.Port = 20000\nstage := egress.NewUDPStage(in, cfg)\n</code></pre>","path":["Built-in Stages","Egress","UDP Egress"],"tags":[]},{"location":"stages/egress/udp/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Egress","UDP Egress"],"tags":[]},{"location":"stages/egress/udp/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body requirement: <code>message.Serializable</code>.</p> <p>The stage sends the byte slice returned by <code>GetBytes()</code> as one UDP datagram.</p> <p>This egress stage produces no downstream output message.</p>","path":["Built-in Stages","Egress","UDP Egress"],"tags":[]},{"location":"stages/egress/udp/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling. <code>IPAddr</code> <code>\"127.0.0.1\"</code> Destination address. <code>Port</code> <code>20000</code> Destination UDP port.","path":["Built-in Stages","Egress","UDP Egress"],"tags":[]},{"location":"stages/egress/udp/#internals","level":2,"title":"Internals","text":"<p>The stage uses Go's standard <code>net</code> package. Initialization creates a connected <code>net.UDPConn</code> with <code>net.DialUDP</code>; each worker writes the byte slice returned by <code>GetBytes()</code> and records delivered-byte metrics.</p>","path":["Built-in Stages","Egress","UDP Egress"],"tags":[]},{"location":"stages/ingress/","level":1,"title":"Ingress Stages","text":"<p>Ingress stages produce messages and write them to an output connector. They do not accept a running mode because each source owns its own runner.</p> Stage Constructor Source Generic Loop <code>ingress.NewGenericLoopStage</code> User-defined producer loop Generic Fan-In <code>ingress.NewGenericFanInStage</code> User-defined source fan-in Ticker <code>ingress.NewTickerStage</code> Periodic timer TCP <code>ingress.NewTCPStage</code> TCP listener UDP <code>ingress.NewUDPStage</code> UDP socket File <code>ingress.NewFileStage</code> Watched files Kafka <code>ingress.NewKafkaStage</code> Kafka consumer group eBPF <code>ingress.NewEBPFStage</code> eBPF ring buffer","path":["Built-in Stages","Ingress","Ingress Stages"],"tags":[]},{"location":"stages/ingress/ebpf/","level":1,"title":"eBPF Ingress","text":"<p><code>EBPFStage</code> reads records from an eBPF ring buffer and decodes each record into an <code>EBPFMessage[T]</code>.</p> <pre><code>cfg := ingress.NewEBPFConfig(loadBpf, attachBpf, ringBuffer)\nstage := ingress.NewEBPFStage(out, cfg)\n</code></pre>","path":["Built-in Stages","Ingress","eBPF Ingress"],"tags":[]},{"location":"stages/ingress/ebpf/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Ingress","eBPF Ingress"],"tags":[]},{"location":"stages/ingress/ebpf/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*ingress.EBPFMessage[T]</code>.</p> Field Description <code>Data</code> Parsed ring-buffer record as the user-provided Go type <code>T</code>. <p>Additional interfaces: none. <code>EBPFMessage[T]</code> only implements the standard <code>message.Body</code> contract.</p>","path":["Built-in Stages","Ingress","eBPF Ingress"],"tags":[]},{"location":"stages/ingress/ebpf/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>LoadFn</code> required Function generated by <code>bpf2go</code> that returns an eBPF collection spec. <code>LinkFn</code> required Function that attaches loaded objects to a hook and returns a <code>link.Link</code>. <code>RingBufferGetter</code> required Function that returns the ring-buffer map from loaded objects. <code>UseUnsafe</code> <code>false</code> If true, decode records with an unsafe struct cast instead of <code>binary.Read</code>. <code>CollectionOptions</code> <code>nil</code> Optional Cilium eBPF collection load options.","path":["Built-in Stages","Ingress","eBPF Ingress"],"tags":[]},{"location":"stages/ingress/ebpf/#internals","level":2,"title":"Internals","text":"<p>The stage uses <code>github.com/cilium/ebpf</code>, <code>link</code>, <code>ringbuf</code>, and <code>rlimit</code>. Initialization removes memlock limits, loads and attaches the eBPF objects, opens a <code>ringbuf.Reader</code>, then decodes each <code>RawSample</code>. The safe path uses <code>encoding/binary.Read</code> with little endian byte order.</p>","path":["Built-in Stages","Ingress","eBPF Ingress"],"tags":[]},{"location":"stages/ingress/file/","level":1,"title":"File Ingress","text":"<p><code>FileStage</code> watches directories and emits file chunks from existing and changed files.</p> <pre><code>cfg := ingress.NewFileConfig()\ncfg.WatchedDirs = []string{\"./data/in\"}\nstage := ingress.NewFileStage(out, cfg)\n</code></pre>","path":["Built-in Stages","Ingress","File Ingress"],"tags":[]},{"location":"stages/ingress/file/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Ingress","File Ingress"],"tags":[]},{"location":"stages/ingress/file/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*ingress.FileMessage</code>.</p> Field Description <code>Path</code> Path of the file that produced the chunk. <code>Chunk</code> Chunk bytes read from the file. <code>ChunkSize</code> Length of the chunk. <code>Offset</code> Offset of the chunk from the beginning of the file. <code>DelimiterFound</code> Whether <code>ChunkDelim</code> was found while growing the chunk. <p>Additional interfaces: <code>message.Serializable</code>. <code>GetBytes()</code> returns <code>Chunk</code>.</p>","path":["Built-in Stages","Ingress","File Ingress"],"tags":[]},{"location":"stages/ingress/file/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>WatchedDirs</code> <code>[]string{\".\"}</code> Directories watched for file events. <code>ChunkSize</code> <code>4096</code> Initial chunk read size. <code>CheckChunkDelim</code> <code>true</code> Grow a chunk until <code>ChunkDelim</code>, EOF, or <code>MaxChunkSize</code>. <code>ChunkDelim</code> <code>'\\n'</code> Delimiter byte used when delimiter checking is enabled. <code>MaxChunkSize</code> <code>32 * 1024</code> Maximum chunk size when growing to a delimiter. <code>ForceReRead</code> <code>false</code> If true, restart from offset <code>0</code> when a reader reopens a file. <code>CloseDebounce</code> <code>time.Second</code> Delay before closing an idle reader after EOF.","path":["Built-in Stages","Ingress","File Ingress"],"tags":[]},{"location":"stages/ingress/file/#internals","level":2,"title":"Internals","text":"<p>The stage uses <code>github.com/fsnotify/fsnotify</code> to watch directories. It reads existing files once at startup, starts one reader per file, uses <code>bufio</code> and <code>os.File</code> from the standard library for reads, and fan-ins chunks through an internal queue before writing to the output connector.</p>","path":["Built-in Stages","Ingress","File Ingress"],"tags":[]},{"location":"stages/ingress/generic-fan-in/","level":1,"title":"Generic Fan-In Ingress","text":"<p><code>GenericFanInStage</code> accepts arbitrary source data, starts one producer loop per source, and fan-ins generated messages to the pipeline.</p> <pre><code>cfg := ingress.NewGenericFanInConfig(\"accept_clients\")\ncfg.QueueSize = 1024\nstage := ingress.NewGenericFanInStage(handler, out, cfg)\n</code></pre>","path":["Built-in Stages","Ingress","Generic Fan-In Ingress"],"tags":[]},{"location":"stages/ingress/generic-fan-in/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Ingress","Generic Fan-In Ingress"],"tags":[]},{"location":"stages/ingress/generic-fan-in/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: the handler's <code>Out</code> type parameter.</p> <p><code>GenericFanInStage</code> only requires <code>Out</code> to implement <code>message.Body</code>. Any additional interfaces depend on the concrete output body type returned by the handler.</p> <p><code>HandleData</code> returns a complete <code>*message.Message[Out]</code>, so the handler controls the message body and any message metadata it sets before writing to the next stage.</p>","path":["Built-in Stages","Ingress","Generic Fan-In Ingress"],"tags":[]},{"location":"stages/ingress/generic-fan-in/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Name</code> constructor arg, or <code>\"generic\"</code> when empty Stage name used in telemetry and the pipeline graph. <code>QueueSize</code> <code>512</code> Internal fan-in queue size between per-source producer loops and the output connector.","path":["Built-in Stages","Ingress","Generic Fan-In Ingress"],"tags":[]},{"location":"stages/ingress/generic-fan-in/#handler","level":2,"title":"Handler","text":"<p>The handler implements <code>Init(ctx)</code>, <code>HandleSource(ctx)</code>, <code>OnRunContextDone()</code>, <code>HandleData(ctx, data)</code>, <code>OnHandleDataContextDone()</code>, <code>Close()</code>, and <code>SetTelemetry(tel)</code>. Embed <code>ingress.GenericFanInHandlerBase</code> when you only need to implement source and data handling.</p> <p><code>HandleSource</code> is called in the main loop and returns arbitrary <code>Data</code> for a new producer goroutine. For example, a TCP-like source can return an accepted connection.</p> <p><code>HandleData</code> is called repeatedly in the per-source goroutine and returns messages to emit. Return <code>ingress.ErrQuitLoop</code> from <code>HandleSource</code> to stop the main loop, or from <code>HandleData</code> to stop only that source loop. Other errors are logged and the relevant loop continues.</p>","path":["Built-in Stages","Ingress","Generic Fan-In Ingress"],"tags":[]},{"location":"stages/ingress/generic-fan-in/#internals","level":2,"title":"Internals","text":"<p>The stage uses a custom fan-in ingress runner. The main loop discovers source data, each source goroutine writes generated messages to an internal fan-in queue, and an output bridge drains that queue into the stage output connector.</p>","path":["Built-in Stages","Ingress","Generic Fan-In Ingress"],"tags":[]},{"location":"stages/ingress/generic-loop/","level":1,"title":"Generic Loop Ingress","text":"<p><code>GenericLoopStage</code> runs a user-provided loop that creates messages for the pipeline.</p> <pre><code>cfg := ingress.NewGenericConfig(\"read_sensor\")\nstage := ingress.NewGenericLoopStage(handler, out, cfg)\n</code></pre>","path":["Built-in Stages","Ingress","Generic Loop Ingress"],"tags":[]},{"location":"stages/ingress/generic-loop/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Ingress","Generic Loop Ingress"],"tags":[]},{"location":"stages/ingress/generic-loop/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: the handler's <code>Out</code> type parameter.</p> <p><code>GenericLoopStage</code> only requires <code>Out</code> to implement <code>message.Body</code>. Any additional interfaces depend on the concrete output body type returned by the handler.</p> <p>The handler returns a complete <code>*message.Message[Out]</code>, so it controls the message body and any message metadata it sets before writing to the next stage.</p>","path":["Built-in Stages","Ingress","Generic Loop Ingress"],"tags":[]},{"location":"stages/ingress/generic-loop/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Name</code> constructor arg, or <code>\"generic\"</code> when empty Stage name used in telemetry and the pipeline graph.","path":["Built-in Stages","Ingress","Generic Loop Ingress"],"tags":[]},{"location":"stages/ingress/generic-loop/#handler","level":2,"title":"Handler","text":"<p>The handler implements <code>Init(ctx)</code>, <code>Handle(ctx)</code>, <code>OnRunContextDone()</code>, <code>Close()</code>, and <code>SetTelemetry(tel)</code>. Embed <code>ingress.GenericLoopHandlerBase</code> when you only need to implement <code>Handle</code>.</p> <p><code>Handle</code> is called repeatedly until the stage context is canceled or it returns <code>ingress.ErrQuitLoop</code>. Other errors are logged and the loop continues.</p> <p><code>OnRunContextDone</code> is called when the running context is canceled, so blocking source logic can be interrupted or cleaned up.</p>","path":["Built-in Stages","Ingress","Generic Loop Ingress"],"tags":[]},{"location":"stages/ingress/generic-loop/#internals","level":2,"title":"Internals","text":"<p>The stage uses a custom single ingress runner. Each successful handler call writes the returned message to the output connector. If the output write fails, the message is destroyed and the runner continues.</p>","path":["Built-in Stages","Ingress","Generic Loop Ingress"],"tags":[]},{"location":"stages/ingress/kafka/","level":1,"title":"Kafka Ingress","text":"<p><code>KafkaStage</code> consumes Kafka messages and emits <code>KafkaMessage</code> values.</p> <pre><code>cfg := ingress.DefaultKafkaConfig(\"events\")\ncfg.GroupID = \"goccia-consumer\"\nstage := ingress.NewKafkaStage(out, cfg)\n</code></pre>","path":["Built-in Stages","Ingress","Kafka Ingress"],"tags":[]},{"location":"stages/ingress/kafka/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Ingress","Kafka Ingress"],"tags":[]},{"location":"stages/ingress/kafka/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*ingress.KafkaMessage</code>.</p> Field Description <code>Topic</code> Kafka topic that produced the record. <code>Key</code> Kafka record key. <code>Value</code> Kafka record value. <code>Headers</code> Kafka headers, including any trace context extracted by the stage. <p>Additional interfaces: <code>message.Serializable</code>. <code>GetBytes()</code> returns <code>Value</code>.</p>","path":["Built-in Stages","Ingress","Kafka Ingress"],"tags":[]},{"location":"stages/ingress/kafka/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Brokers</code> <code>[]string{\"localhost:9092\"}</code> Kafka broker addresses. <code>GroupID</code> <code>\"group\"</code> Consumer group id. <code>Topics</code> constructor args Topics consumed by the group. No topic is added unless you pass one. <code>Dialer</code> <code>nil</code> Optional <code>kafka.Dialer</code>. <code>QueueCapacity</code> <code>100</code> Internal kafka-go reader queue capacity. <code>MinBytes</code> <code>1</code> Minimum fetch size accepted by the consumer. <code>MaxBytes</code> <code>1 &lt;&lt; 20</code> Maximum fetch size accepted by the consumer. <code>MaxWait</code> <code>10 * time.Second</code> Maximum wait for broker fetch data. <code>ReadBatchTimeout</code> <code>10 * time.Second</code> Timeout while reading from a fetched batch. <code>GroupBalancers</code> range, round-robin Consumer group balancing strategies. <code>HeartbeatInterval</code> <code>3 * time.Second</code> Consumer group heartbeat interval. <code>CommitInterval</code> <code>0</code> Offset commit interval. <code>0</code> commits synchronously. <code>PartitionWatchInterval</code> <code>5 * time.Second</code> Partition-change polling interval. <code>WatchPartitionChanges</code> <code>false</code> Rebalance on partition changes when enabled. <code>SessionTimeout</code> <code>30 * time.Second</code> Consumer group session timeout. <code>RebalanceTimeout</code> <code>30 * time.Second</code> Rebalance join timeout. <code>JoinGroupBackoff</code> <code>5 * time.Second</code> Backoff before rejoining after an error. <code>RetentionTime</code> <code>7 * 24 * time.Hour</code> Consumer group retention time. <code>StartOffset</code> <code>kafka.FirstOffset</code> Offset used for partitions without committed offsets. <code>ReadBackoffMin</code> <code>100 * time.Millisecond</code> Minimum read backoff. <code>ReadBackoffMax</code> <code>1 * time.Second</code> Maximum read backoff. <code>IsolationLevel</code> <code>kafka.ReadUncommitted</code> Transactional record visibility. <code>MaxAttempts</code> <code>3</code> Connection attempt limit.","path":["Built-in Stages","Ingress","Kafka Ingress"],"tags":[]},{"location":"stages/ingress/kafka/#internals","level":2,"title":"Internals","text":"<p>The stage uses <code>github.com/segmentio/kafka-go</code>. It creates a <code>kafka.Reader</code>, calls <code>ReadMessage(ctx)</code> in the runner, extracts trace context from Kafka headers when present, and writes received messages to the output connector.</p>","path":["Built-in Stages","Ingress","Kafka Ingress"],"tags":[]},{"location":"stages/ingress/tcp/","level":1,"title":"TCP Ingress","text":"<p><code>TCPStage</code> listens for TCP connections and emits one <code>TCPMessage</code> per framed message.</p> <pre><code>cfg := ingress.NewTCPConfig()\ncfg.Port = 20000\nstage := ingress.NewTCPStage(out, &amp;cfg)\n</code></pre>","path":["Built-in Stages","Ingress","TCP Ingress"],"tags":[]},{"location":"stages/ingress/tcp/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Ingress","TCP Ingress"],"tags":[]},{"location":"stages/ingress/tcp/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*ingress.TCPMessage</code>.</p> Field Description <code>RemoteAddr</code> Remote peer address for the accepted connection. <code>Message</code> Framed payload bytes. <code>MessageSize</code> Payload length in bytes. <p>Additional interfaces: <code>message.Serializable</code>. <code>GetBytes()</code> returns <code>Message</code>, so TCP ingress output can feed serializable processors and byte-oriented egress stages.</p>","path":["Built-in Stages","Ingress","TCP Ingress"],"tags":[]},{"location":"stages/ingress/tcp/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>IPAddr</code> <code>\"0.0.0.0\"</code> Local address to bind. <code>Port</code> <code>20000</code> Local TCP port. <code>BufferSize</code> <code>4096</code> constant Read buffer size. Set this explicitly when using <code>NewTCPConfig()</code>, because the constructor currently leaves it at Go's zero value. <code>ReadTimeout</code> <code>10 * time.Second</code> Per-read timeout for accepted connections. <code>FramingMode</code> <code>TCPFramingModeDelimited</code> Message framing strategy. <code>MaxMessageSize</code> <code>4 &lt;&lt; 20</code> Maximum accumulated frame size before closing a connection. <code>Delimiter</code> <code>\"\\r\\n\"</code> Delimiter used by delimited framing. <code>HeaderLen</code> <code>16</code> constant Header size for length-prefixed framing. <code>MessageLengthFieldLen</code> none Length-field width for length-prefixed framing. <code>MessageLengthFieldOffset</code> <code>0</code> Offset of the length field inside the header. <code>MessageLengthFieldEndianess</code> <code>LittleEndian</code> zero value Byte order for the length field. <code>OutputQueueSize</code> <code>512</code> Fan-in queue between connection goroutines and the output connector.","path":["Built-in Stages","Ingress","TCP Ingress"],"tags":[]},{"location":"stages/ingress/tcp/#framing","level":2,"title":"Framing","text":"<p>Use <code>TCPFramingModeDelimited</code> for delimiter-separated messages. Use <code>TCPFramingModeLengthPrefixed</code> when a fixed header contains the payload length. For length-prefixed mode, set <code>HeaderLen</code>, <code>MessageLengthFieldLen</code>, <code>MessageLengthFieldOffset</code>, and <code>MessageLengthFieldEndianess</code>.</p>","path":["Built-in Stages","Ingress","TCP Ingress"],"tags":[]},{"location":"stages/ingress/tcp/#internals","level":2,"title":"Internals","text":"<p>The stage uses Go's standard <code>net</code> package. It owns a <code>net.TCPListener</code>, starts one goroutine per accepted connection, uses a buffer <code>sync.Pool</code>, and fan-ins decoded messages through an internal queue before writing to the output connector.</p>","path":["Built-in Stages","Ingress","TCP Ingress"],"tags":[]},{"location":"stages/ingress/ticker/","level":1,"title":"Ticker Ingress","text":"<p><code>TickerStage</code> emits a <code>TickerMessage</code> at a fixed interval.</p> <pre><code>cfg := ingress.NewTickerConfig()\nstage := ingress.NewTickerStage(out, cfg)\n</code></pre>","path":["Built-in Stages","Ingress","Ticker Ingress"],"tags":[]},{"location":"stages/ingress/ticker/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Ingress","Ticker Ingress"],"tags":[]},{"location":"stages/ingress/ticker/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*ingress.TickerMessage</code>.</p> Field Description <code>TickNumber</code> Tick counter, starting at <code>1</code> and incrementing while the stage runs. <p>Additional interfaces: none. <code>TickerMessage</code> only implements the standard <code>message.Body</code> contract.</p>","path":["Built-in Stages","Ingress","Ticker Ingress"],"tags":[]},{"location":"stages/ingress/ticker/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Interval</code> <code>100 * time.Millisecond</code> Time between generated ticks. Must be positive.","path":["Built-in Stages","Ingress","Ticker Ingress"],"tags":[]},{"location":"stages/ingress/ticker/#internals","level":2,"title":"Internals","text":"<p>The runner uses Go's standard <code>time.Ticker</code>. Each tick creates a new message, sets receive time and timestamp to <code>time.Now()</code>, records telemetry, and writes to the output connector.</p>","path":["Built-in Stages","Ingress","Ticker Ingress"],"tags":[]},{"location":"stages/ingress/udp/","level":1,"title":"UDP Ingress","text":"<p><code>UDPStage</code> listens on a UDP socket and emits one <code>UDPMessage</code> per datagram.</p> <pre><code>cfg := ingress.NewUDPConfig()\ncfg.Port = 20000\nstage := ingress.NewUDPStage(out, cfg)\n</code></pre>","path":["Built-in Stages","Ingress","UDP Ingress"],"tags":[]},{"location":"stages/ingress/udp/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Ingress","UDP Ingress"],"tags":[]},{"location":"stages/ingress/udp/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*ingress.UDPMessage</code>.</p> Field Description <code>Payload</code> Backing byte slice for the datagram payload. <code>PayloadSize</code> Number of received payload bytes. <p>Additional interfaces: <code>message.Serializable</code>. <code>GetBytes()</code> returns <code>Payload[:PayloadSize]</code>. Messages are pooled and returned to the pool by <code>Destroy()</code>.</p>","path":["Built-in Stages","Ingress","UDP Ingress"],"tags":[]},{"location":"stages/ingress/udp/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>IPAddr</code> <code>\"0.0.0.0\"</code> Local address to bind. <code>Port</code> <code>20000</code> Local UDP port. <code>BufferSize</code> <code>1472</code> Receive buffer size and default payload capacity.","path":["Built-in Stages","Ingress","UDP Ingress"],"tags":[]},{"location":"stages/ingress/udp/#internals","level":2,"title":"Internals","text":"<p>The stage uses Go's standard <code>net</code> package with <code>net.ListenUDP</code>. The runner blocks on <code>Read</code>, copies each datagram into a pooled message, records receive metrics, and writes to the output connector.</p>","path":["Built-in Stages","Ingress","UDP Ingress"],"tags":[]},{"location":"stages/processor/","level":1,"title":"Processor Stages","text":"<p>Processor stages read from an input connector and write to an output connector.</p> Family Stage Constructor Capability Purpose Generic Generic <code>processor.NewGenericStage</code> Pool-capable User-defined transform Generic Filter <code>processor.NewFilterStage</code> Pool-capable Drop messages by predicate Generic Tee <code>processor.NewTeeStage</code> Single runner Clone one stream to many outputs CSV Decoder <code>processor.NewCSVDecoderStage</code> Pool-capable Bytes to typed CSV rows CSV Encoder <code>processor.NewCSVEncoderStage</code> Pool-capable Typed CSV rows to bytes CAN CAN <code>processor.NewCANStage</code> Pool-capable Raw CAN frames to decoded signals Cannelloni Decoder <code>processor.NewCannelloniDecoderStage</code> Pool-capable Cannelloni bytes to CAN frames Cannelloni Encoder <code>processor.NewCannelloniEncoderStage</code> Pool-capable CAN frames to Cannelloni bytes Ordering ROB <code>processor.NewROBStage</code> Single runner Reorder by sequence number","path":["Built-in Stages","Processor","Processor Stages"],"tags":[]},{"location":"stages/processor/can/","level":1,"title":"CAN Processor","text":"<p><code>CANStage</code> decodes raw CAN messages into typed signal values.</p> <p>Pool-capable</p> <pre><code>cfg := processor.NewCANConfig(goccia.StageRunningModePool)\ncfg.Messages = []*acmelib.Message{speedMsg, statusMsg}\nstage := processor.NewCANStage(in, out, cfg)\n</code></pre>","path":["Built-in Stages","Processor","CAN Processor"],"tags":[]},{"location":"stages/processor/can/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Processor","CAN Processor"],"tags":[]},{"location":"stages/processor/can/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body requirement: <code>processor.CANMessageCarrier</code>.</p> <p><code>CANMessageCarrier</code> exposes <code>GetRawMessages() []CANRawMessage</code>.</p>","path":["Built-in Stages","Processor","CAN Processor"],"tags":[]},{"location":"stages/processor/can/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*processor.CANMessage</code>.</p> Field Description <code>Signals</code> Decoded signal values. <code>SignalCount</code> Number of decoded signals. <p>Each <code>CANSignal</code> carries <code>CANID</code>, <code>Name</code>, <code>RawValue</code>, <code>Type</code>, and one type-specific value field: <code>ValueFlag</code>, <code>ValueInt</code>, <code>ValueFloat</code>, or <code>ValueEnum</code>.</p> <p>Additional interfaces: none. <code>CANMessage</code> only implements the standard <code>message.Body</code> contract.</p>","path":["Built-in Stages","Processor","CAN Processor"],"tags":[]},{"location":"stages/processor/can/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling. <code>Messages</code> empty CAN message definitions from <code>github.com/squadracorsepolito/acmelib</code>.","path":["Built-in Stages","Processor","CAN Processor"],"tags":[]},{"location":"stages/processor/can/#internals","level":2,"title":"Internals","text":"<p>The stage uses Goccia's generic worker-backed processor runner. Decoding is delegated to <code>acmelib</code> signal layouts, keyed by CAN id. Unknown CAN ids produce no decoded signals.</p>","path":["Built-in Stages","Processor","CAN Processor"],"tags":[]},{"location":"stages/processor/cannelloni-decoder/","level":1,"title":"Cannelloni Decoder Processor","text":"<p><code>CannelloniDecoderStage</code> decodes Cannelloni protocol bytes into <code>CannelloniMessage</code> values carrying raw CAN messages.</p> <p>Pool-capable</p> <pre><code>cfg := processor.NewCannelloniConfig(goccia.StageRunningModePool)\nstage := processor.NewCannelloniDecoderStage(in, out, cfg)\n</code></pre>","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Decoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-decoder/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Decoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-decoder/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body requirement: <code>message.Serializable</code>.</p> <p>The decoder reads the Cannelloni frame bytes returned by <code>GetBytes()</code>.</p>","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Decoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-decoder/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*processor.CannelloniMessage</code>.</p> Field or method Description <code>Messages</code> Raw CAN messages decoded from the Cannelloni frame. <code>MessageCount</code> Number of raw CAN messages. <code>GetSequenceNumber()</code> Frame sequence number exposed for reordering. <code>GetRawMessages()</code> Raw CAN messages exposed for <code>CANStage</code>. <p>Additional interfaces: <code>message.ReOrderable</code> and <code>processor.CANMessageCarrier</code>. This means decoder output can pass through <code>ROBStage</code> and then into <code>CANStage</code>.</p>","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Decoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-decoder/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling.","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Decoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-decoder/#internals","level":2,"title":"Internals","text":"<p>The decoder uses the generic worker-backed processor runner. It parses the Cannelloni frame manually with big-endian binary reads, validates basic frame lengths, and converts frame messages into raw CAN messages.</p>","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Decoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-encoder/","level":1,"title":"Cannelloni Encoder Processor","text":"<p><code>CannelloniEncoderStage</code> encodes <code>CannelloniMessage</code> values into Cannelloni protocol bytes.</p> <p>Pool-capable</p> <pre><code>cfg := processor.NewCannelloniConfig(goccia.StageRunningModeSingle)\nstage := processor.NewCannelloniEncoderStage(in, out, cfg)\n</code></pre>","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Encoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-encoder/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Encoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-encoder/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body type: <code>*processor.CannelloniMessage</code>.</p> <p>Additional input interfaces: <code>message.ReOrderable</code> and <code>processor.CANMessageCarrier</code>, as implemented by <code>CannelloniMessage</code>.</p>","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Encoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-encoder/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*processor.CannelloniEncodedMessage</code>.</p> Field or method Description <code>GetBytes()</code> Encoded Cannelloni frame bytes. <p>Additional interfaces: <code>message.Serializable</code>. The encoded payload field is private; use <code>GetBytes()</code> to pass it to byte-oriented egress stages.</p>","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Encoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-encoder/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling.","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Encoder Processor"],"tags":[]},{"location":"stages/processor/cannelloni-encoder/#internals","level":2,"title":"Internals","text":"<p>The encoder uses the generic worker-backed processor runner. It writes Cannelloni frame headers and raw CAN messages manually with big-endian binary encoding, using version <code>1</code> and opcode <code>0</code>.</p>","path":["Built-in Stages","Processor","Cannelloni","Cannelloni Encoder Processor"],"tags":[]},{"location":"stages/processor/csv-decoder/","level":1,"title":"CSV Decoder Processor","text":"<p><code>CSVDecoderStage</code> converts serializable byte messages into <code>CSVMessage</code> rows.</p> <p>Pool-capable</p> <pre><code>cfg := processor.NewCSVConfig(goccia.StageRunningModeSingle)\ncfg.AddColumnDef(processor.NewCSVColumnDef(\"frame\", processor.CSVColumnTypeInt))\ncfg.AddColumnDef(processor.NewCSVColumnDef(\"ts\", processor.CSVColumnTypeTimestamp))\nstage := processor.NewCSVDecoderStage(in, out, cfg)\n</code></pre>","path":["Built-in Stages","Processor","CSV","CSV Decoder Processor"],"tags":[]},{"location":"stages/processor/csv-decoder/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Processor","CSV","CSV Decoder Processor"],"tags":[]},{"location":"stages/processor/csv-decoder/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body requirement: <code>message.Serializable</code>.</p> <p>The decoder reads the byte slice returned by <code>GetBytes()</code>.</p>","path":["Built-in Stages","Processor","CSV","CSV Decoder Processor"],"tags":[]},{"location":"stages/processor/csv-decoder/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*processor.CSVMessage</code>.</p> Field Description <code>Rows</code> Decoded rows, where each row is a list of <code>*CSVColumn</code>. <code>RowCount</code> Row-count field carried by the message type. <p>Each <code>CSVColumn</code> carries <code>Name</code>, <code>Type</code>, <code>IsDataValid</code>, and one type-specific value field: <code>StringValue</code>, <code>IntValue</code>, <code>FloatValue</code>, <code>BoolValue</code>, or <code>TimestampValue</code>.</p> <p>Additional interfaces: none. <code>CSVMessage</code> only implements the standard <code>message.Body</code> contract.</p>","path":["Built-in Stages","Processor","CSV","CSV Decoder Processor"],"tags":[]},{"location":"stages/processor/csv-decoder/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling. <code>Columns</code> empty Ordered list of <code>CSVColumnDef</code> values. <code>CSVColumnDef.Name</code> constructor arg Column name in decoded output. <code>CSVColumnDef.Type</code> constructor arg <code>String</code>, <code>Int</code>, <code>Float</code>, <code>Bool</code>, or <code>Timestamp</code>. <code>CSVColumnDef.TimestampLayout</code> <code>time.RFC3339</code> Optional layout for timestamp parsing.","path":["Built-in Stages","Processor","CSV","CSV Decoder Processor"],"tags":[]},{"location":"stages/processor/csv-decoder/#internals","level":2,"title":"Internals","text":"<p>The decoder uses Goccia's generic worker-backed processor runner. The parser is a small in-package comma/newline parser backed by <code>strings.Builder</code>, <code>strconv</code>, and <code>time.Parse</code>; it does not implement full RFC 4180 quoting or escaping.</p>","path":["Built-in Stages","Processor","CSV","CSV Decoder Processor"],"tags":[]},{"location":"stages/processor/csv-encoder/","level":1,"title":"CSV Encoder Processor","text":"<p><code>CSVEncoderStage</code> converts <code>CSVMessage</code> rows into <code>CSVEncodedMessage</code> bytes.</p> <p>Pool-capable</p> <pre><code>cfg := processor.NewCSVConfig(goccia.StageRunningModeSingle)\ncfg.AddColumnDef(processor.NewCSVColumnDef(\"frame\", processor.CSVColumnTypeInt))\nstage := processor.NewCSVEncoderStage(in, out, cfg)\n</code></pre>","path":["Built-in Stages","Processor","CSV","CSV Encoder Processor"],"tags":[]},{"location":"stages/processor/csv-encoder/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Processor","CSV","CSV Encoder Processor"],"tags":[]},{"location":"stages/processor/csv-encoder/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body type: <code>*processor.CSVMessage</code>.</p> <p>Additional input interfaces: none beyond the standard <code>message.Body</code> contract.</p>","path":["Built-in Stages","Processor","CSV","CSV Encoder Processor"],"tags":[]},{"location":"stages/processor/csv-encoder/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: <code>*processor.CSVEncodedMessage</code>.</p> Field Description <code>Data</code> Encoded CSV bytes. <p>Additional interfaces: <code>message.Serializable</code>. <code>GetBytes()</code> returns <code>Data</code>.</p>","path":["Built-in Stages","Processor","CSV","CSV Encoder Processor"],"tags":[]},{"location":"stages/processor/csv-encoder/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling. <code>Columns</code> empty Ordered list of columns to write. Missing or invalid values use type-specific defaults. <code>CSVColumnDef.TimestampLayout</code> <code>time.RFC3339</code> Layout used when formatting timestamp columns.","path":["Built-in Stages","Processor","CSV","CSV Encoder Processor"],"tags":[]},{"location":"stages/processor/csv-encoder/#internals","level":2,"title":"Internals","text":"<p>The encoder uses Goccia's generic worker-backed processor runner. It builds rows with <code>strings.Builder</code>, formats scalar values with <code>strconv</code>, appends a newline after each row, and emits one encoded byte message per input message.</p>","path":["Built-in Stages","Processor","CSV","CSV Encoder Processor"],"tags":[]},{"location":"stages/processor/filter/","level":1,"title":"Filter Processor","text":"<p><code>FilterStage</code> keeps messages whose predicate returns <code>true</code> and drops messages whose predicate returns <code>false</code>.</p> <p>Pool-capable</p> <pre><code>cfg := processor.NewFilterConfig(goccia.StageRunningModePool)\nstage := processor.NewFilterStage(keepFn, in, out, cfg)\n</code></pre>","path":["Built-in Stages","Processor","Filter Processor"],"tags":[]},{"location":"stages/processor/filter/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Processor","Filter Processor"],"tags":[]},{"location":"stages/processor/filter/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body type: <code>T</code>.</p> <p>Additional input interfaces: none required beyond <code>message.Body</code>. If <code>T</code> implements <code>message.Serializable</code>, <code>message.ReOrderable</code>, or another interface, that interface is preserved for kept messages.</p>","path":["Built-in Stages","Processor","Filter Processor"],"tags":[]},{"location":"stages/processor/filter/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: the same <code>T</code> body received from the input connector, when the predicate returns <code>true</code>.</p> <p>Additional interfaces: preserved from <code>T</code>. The stage does not create a new body or add interfaces; a serializable input remains serializable, a reorderable input remains reorderable, and a filtered-out message is marked dropped.</p>","path":["Built-in Stages","Processor","Filter Processor"],"tags":[]},{"location":"stages/processor/filter/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling. <p>The predicate is configured through the <code>filterFn func(T) bool</code> constructor argument, not through the config object.</p>","path":["Built-in Stages","Processor","Filter Processor"],"tags":[]},{"location":"stages/processor/filter/#internals","level":2,"title":"Internals","text":"<p>The stage uses the generic worker-backed processor runner. The worker preserves kept messages by forwarding the original envelope. Filtered messages are marked dropped and counted in filter metrics.</p>","path":["Built-in Stages","Processor","Filter Processor"],"tags":[]},{"location":"stages/processor/generic/","level":1,"title":"Generic Processor","text":"<p><code>GenericStage</code> runs a user-provided handler for each input message.</p> <p>Pool-capable</p> <pre><code>cfg := processor.NewGenericConfig(goccia.StageRunningModePool)\ncfg.Name = \"decode_payload\"\nstage := processor.NewGenericStage(handler, in, out, cfg)\n</code></pre>","path":["Built-in Stages","Processor","Generic Processor"],"tags":[]},{"location":"stages/processor/generic/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Processor","Generic Processor"],"tags":[]},{"location":"stages/processor/generic/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body type: the handler's <code>In</code> type parameter.</p> <p><code>GenericStage</code> only requires <code>In</code> to implement <code>message.Body</code>. Any additional interfaces depend on the concrete input body type.</p>","path":["Built-in Stages","Processor","Generic Processor"],"tags":[]},{"location":"stages/processor/generic/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: the handler's <code>Out</code> type parameter.</p> <p><code>GenericStage</code> only requires the returned body to implement <code>message.Body</code>. Additional interfaces are not added by the stage; if <code>Out</code> also implements <code>message.Serializable</code>, <code>message.ReOrderable</code>, or an application-specific interface, that behavior comes from the concrete type returned by the handler.</p>","path":["Built-in Stages","Processor","Generic Processor"],"tags":[]},{"location":"stages/processor/generic/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>Stage.RunningMode</code> constructor arg <code>StageRunningModeSingle</code> or <code>StageRunningModePool</code>. <code>Stage.Pool</code> default pool when pool mode is selected Worker counts, queue sizes, and auto-scaling. <code>Name</code> <code>\"generic\"</code> Stage name used in telemetry and the pipeline graph.","path":["Built-in Stages","Processor","Generic Processor"],"tags":[]},{"location":"stages/processor/generic/#handler","level":2,"title":"Handler","text":"<p>The handler implements <code>Init(ctx)</code>, <code>Handle(ctx, msgIn)</code>, <code>Close()</code>, and <code>SetTelemetry(tel)</code>. Embed <code>processor.GenericHandlerBase</code> when you only need to implement <code>Handle</code>.</p>","path":["Built-in Stages","Processor","Generic Processor"],"tags":[]},{"location":"stages/processor/generic/#internals","level":2,"title":"Internals","text":"<p>The stage uses Goccia's generic worker-backed processor runner. Each worker calls the handler, wraps the returned body in a new message envelope, and saves the handler span for downstream stages.</p>","path":["Built-in Stages","Processor","Generic Processor"],"tags":[]},{"location":"stages/processor/rob/","level":1,"title":"ROB Processor","text":"<p><code>ROBStage</code> reorders messages by sequence number and flushes buffered messages when gaps cannot be filled.</p> <pre><code>cfg := processor.NewROBConfig()\nstage := processor.NewROBStage(in, out, cfg)\n</code></pre>","path":["Built-in Stages","Processor","ROB Processor"],"tags":[]},{"location":"stages/processor/rob/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Processor","ROB Processor"],"tags":[]},{"location":"stages/processor/rob/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body requirement: <code>message.ReOrderable</code>.</p> <p>The stage reads the sequence number from <code>GetSequenceNumber()</code> and copies it into the message envelope before enqueuing into the reorder buffer.</p>","path":["Built-in Stages","Processor","ROB Processor"],"tags":[]},{"location":"stages/processor/rob/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: the same <code>T</code> body received from the input connector, emitted in reordered sequence.</p> <p>Additional interfaces: at least <code>message.ReOrderable</code>, because that is required for input. Any other interfaces implemented by <code>T</code>, such as <code>processor.CANMessageCarrier</code>, are preserved.</p>","path":["Built-in Stages","Processor","ROB Processor"],"tags":[]},{"location":"stages/processor/rob/#configuration","level":2,"title":"Configuration","text":"Field Default Description <code>MaxSeqNum</code> <code>255</code> Maximum possible sequence number before wraparound. <code>PrimaryBufferSize</code> <code>128</code> In-order window buffer size. <code>AuxiliaryBufferSize</code> <code>128</code> Auxiliary buffer for messages outside the primary window. <code>FlushTreshold</code> <code>0.3</code> Auxiliary-buffer fullness threshold that triggers a primary flush. <code>TimeSmootherEnabled</code> <code>true</code> Enables the internal time smoother. <code>EstimatorAlpha</code> <code>0.8</code> Data smoothing factor for the double exponential estimator. <code>EstimatorBeta</code> <code>0.5</code> Trend smoothing factor for the double exponential estimator. <code>ResetTimeout</code> <code>100 * time.Millisecond</code> Read timeout after which pending reorder state may be flushed and reset. <p>The stage is intentionally single-threaded and does not accept a running mode.</p>","path":["Built-in Stages","Processor","ROB Processor"],"tags":[]},{"location":"stages/processor/rob/#internals","level":2,"title":"Internals","text":"<p><code>ROBStage</code> uses a custom runner and the internal <code>rob.ROB</code> implementation. It reads with a timeout, updates the envelope sequence number from the message body, enqueues into primary or auxiliary buffers, emits ordered messages through the output connector, and flushes on shutdown.</p>","path":["Built-in Stages","Processor","ROB Processor"],"tags":[]},{"location":"stages/processor/tee/","level":1,"title":"Tee Processor","text":"<p><code>TeeStage</code> clones each input message to multiple output connectors.</p> <pre><code>stage := processor.NewTeeStage(in, outA, outB, outC)\n</code></pre>","path":["Built-in Stages","Processor","Tee Processor"],"tags":[]},{"location":"stages/processor/tee/#messages","level":2,"title":"Messages","text":"","path":["Built-in Stages","Processor","Tee Processor"],"tags":[]},{"location":"stages/processor/tee/#input-message","level":3,"title":"Input Message","text":"<p>Accepted body type: <code>T</code>.</p> <p>Additional input interfaces: none required beyond <code>message.Body</code>. Any interfaces implemented by <code>T</code> are preserved in the cloned output envelopes.</p>","path":["Built-in Stages","Processor","Tee Processor"],"tags":[]},{"location":"stages/processor/tee/#output-message","level":3,"title":"Output Message","text":"<p>Produced body type: the same <code>T</code> body received from the input connector, cloned to each output connector.</p> <p>Additional interfaces: preserved from <code>T</code>. The stage does not deep-copy the message body or add interfaces. It clones the envelope metadata and increments the message body's reference count, so all outputs share the same body until the envelopes are destroyed.</p>","path":["Built-in Stages","Processor","Tee Processor"],"tags":[]},{"location":"stages/processor/tee/#configuration","level":2,"title":"Configuration","text":"<p><code>TeeStage</code> has no config object. At least one output connector is required.</p>","path":["Built-in Stages","Processor","Tee Processor"],"tags":[]},{"location":"stages/processor/tee/#internals","level":2,"title":"Internals","text":"<p><code>TeeStage</code> uses a custom single runner instead of the generic worker pool. The runner reads one message, creates one clone per output connector, writes each clone, and closes every output connector when the input stream drains.</p>","path":["Built-in Stages","Processor","Tee Processor"],"tags":[]}]}